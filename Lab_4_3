#include <stdio.h>

int main() {
    int sum = 0;

    for (int i = 1; i <= 5; i++) {
        int product = 1;
        for (int j = 1; j <= i; j++) {
            product *= (i - j);
        }
        sum += product;
    }

    printf("Результат: %d\n", sum);
    return 0;
}
